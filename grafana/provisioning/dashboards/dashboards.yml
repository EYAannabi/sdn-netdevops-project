apiVersion: 1

providers:
  - name: SDN NetDevOps Dashboards
    orgId: 1
    folder: SDN NetDevOps
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
