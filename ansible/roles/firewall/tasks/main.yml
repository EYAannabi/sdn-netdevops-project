- name: Wait for topology and switches to be ready
  pause:
    seconds: 60

- name: Apply SDN policies from source of truth
  shell: python3 {{ playbook_dir }}/../scripts/deploy_policies.py
  register: policy_result
  changed_when: true

- name: Show policy deployment result
  debug:
    msg: "{{ policy_result.stdout_lines }}"